/**
 * Build fingerprint, baked into the JS bundle at build time. Used to verify
 * that the bundle actually served (e.g. by Vercel) matches the latest commit —
 * this app has a history of stale `dist`/asset-cache issues. Bump this string
 * in any commit that changes runtime behavior, then check it in the browser
 * console / sources.
 */
export const APP_BUILD = 'submit-answer-flow-2026-09-05';
